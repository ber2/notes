__Scala__ is a [[Strong Type]] programming language that follows the [[FP]] paradigm, although it features a good deal of the [[OOP]] paradigm as well.

It runs on top of the [[JVM]] and is largely compatible with [[Java]].